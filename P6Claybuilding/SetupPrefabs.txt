Setup in P6 VR multiplayer project for prefabs:

Create prefab in blender. Crtl + A -- all transform otherwise things rotate wrong in Unity

export FBX or ABC if animation. In exporter tick ALL TRANSFORM

Import in Unity. Apply collider and realtime view script as component

Apply the RippleMaterial to the Box object.